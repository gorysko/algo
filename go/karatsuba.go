package main

import (
	"strconv"
)

func get_range(num int) (rank int) {
	return len(strconv.Itoa(num))
}

func split_num(num int) (first, second int) {
	num_range := int(get_range(num) / 2.)
}

func karatsuba(first, second int) (result int) {

}

func main() {

}
